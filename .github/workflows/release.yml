name: Release Build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            args: '--target aarch64-apple-darwin'
            name: mac-arm
          - platform: macos-13
            args: '--target x86_64-apple-darwin'
            name: mac-intel
          - platform: windows-latest
            args: ''
            name: windows

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4

      - name: Node.js einrichten
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Rust einrichten
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin' || matrix.platform == 'macos-13' && 'x86_64-apple-darwin' || '' }}

      - name: Rust-Cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Abhängigkeiten installieren
        run: npm ci

      # ── Windows Code-Signing ─────────────────────────────────────────────────
      # Zertifikat aus GitHub Secret importieren (Base64-kodierte .pfx Datei)
      # Secret muss gesetzt sein: WINDOWS_CERTIFICATE + WINDOWS_CERTIFICATE_PASSWORD
      - name: Windows Zertifikat importieren
        if: matrix.platform == 'windows-latest' && env.WINDOWS_CERTIFICATE != ''
        env:
          WINDOWS_CERTIFICATE: ${{ secrets.WINDOWS_CERTIFICATE }}
        shell: pwsh
        run: |
          $certBytes = [Convert]::FromBase64String($env:WINDOWS_CERTIFICATE)
          $certPath = Join-Path $env:RUNNER_TEMP "certificate.pfx"
          [IO.File]::WriteAllBytes($certPath, $certBytes)
          echo "CERT_PATH=$certPath" >> $env:GITHUB_ENV

      - name: Tauri App bauen
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # HMAC-Secret für die Lizenz-Token-Signierung (zur Compile-Zeit eingebettet)
          GRUBEN_HMAC_SECRET: ${{ secrets.GRUBEN_HMAC_SECRET }}
          # Updater-Artefakt-Signierung (latest.json + .sig Dateien)
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ''
          # Windows Signing (nur aktiv wenn Secrets gesetzt)
          TAURI_SIGNING_WINDOWS_CERTIFICATE_FILE: ${{ env.CERT_PATH }}
          TAURI_SIGNING_WINDOWS_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
          # macOS Signing + Notarisierung (nur aktiv wenn Secrets gesetzt):
          #   APPLE_CERTIFICATE          = Base64-kodierte .p12 (Developer ID Application)
          #   APPLE_CERTIFICATE_PASSWORD = Passwort der .p12
          #   APPLE_SIGNING_IDENTITY     = z. B. "Developer ID Application: Name (TEAMID)"
          #   APPLE_ID / APPLE_PASSWORD  = Apple-ID + App-spezifisches Passwort (Notarisierung)
          #   APPLE_TEAM_ID              = Team-ID des Developer-Accounts
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Grubenberechnung ${{ github.ref_name }}'
          releaseBody: |
            ## Grubenberechnung ${{ github.ref_name }}

            ### Installation
            - **Windows**: `.msi` Datei herunterladen und ausführen
            - **Mac (Apple Silicon / M1–M4)**: `_aarch64.dmg` herunterladen
            - **Mac (Intel)**: `_x86_64.dmg` herunterladen

            ### Voraussetzungen
            - Gültiger Lizenzschlüssel erforderlich
            - Internetverbindung für Lizenzaktivierung

            Installierte Apps aktualisieren sich ab dieser Version automatisch.
          releaseDraft: false
          prerelease: false
          includeUpdaterJson: true
          args: ${{ matrix.args }}

      # Zertifikat-Datei nach dem Build sicher löschen
      - name: Zertifikat aufräumen
        if: matrix.platform == 'windows-latest' && env.CERT_PATH != ''
        shell: pwsh
        run: |
          if (Test-Path $env:CERT_PATH) { Remove-Item $env:CERT_PATH -Force }
